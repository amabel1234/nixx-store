import { useState } from 'react';
import { Link } from 'wouter';
import { ArrowLeft, Check, Edit3, Eye, ImagePlus, MessageCircle, Package, Plus, Save, Star, Trash2, X } from 'lucide-react';
import { BrandMark } from '@/components/brand-mark';
import { formatPrice, ProductCard } from '@/components/product-card';
import { Product } from '@/lib/store';
import { useProducts } from '@/hooks/use-products';

type ProductInput = Omit<Product, 'id'>;
const emptyForm: ProductInput = { name: '', price: 0, description: '', image: '', category: 'Sembako', featured: false };
const categories = ['Sembako', 'Minuman', 'Dapur', 'Rumah', 'Camilan', 'Lainnya'];

function ProductForm({ initial, onSave, onCancel, isEditing }: { initial: ProductInput; onSave: (input: ProductInput) => void; onCancel: () => void; isEditing: boolean }) {
  const [form, setForm] = useState<ProductInput>(initial);
  const [imageError, setImageError] = useState(false);
  const canSave = form.name.trim() && form.price > 0 && form.description.trim();
  const setField = <K extends keyof ProductInput>(key: K, value: ProductInput[K]) => setForm((current) => ({ ...current, [key]: value }));
  const preview: Product = { ...form, id: 'preview' };
  return (
    <div className="fixed inset-0 z-50 flex items-end justify-center bg-[hsl(var(--foreground)/.42)] p-0 backdrop-blur-sm sm:items-center sm:p-5" role="dialog" aria-modal="true" data-testid="dialog-product-form">
      <div className="animate-soft-pop max-h-[94dvh] w-full max-w-[950px] overflow-y-auto rounded-t-[26px] bg-[hsl(var(--card))] shadow-[var(--shadow-lg)] sm:rounded-[26px]">
        <div className="flex items-center justify-between border-b border-[hsl(var(--border))] px-5 py-4 sm:px-7"><div><p className="mono-font text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--accent))]">{isEditing ? 'Edit produk' : 'Produk baru'}</p><h2 className="display-font mt-1 text-2xl font-bold tracking-[-.04em] text-[hsl(var(--primary))]">{isEditing ? 'Perbarui detail' : 'Tambah ke etalase'}</h2></div><button onClick={onCancel} className="focus-ring rounded-full p-2 text-[hsl(var(--muted-foreground))] transition hover:bg-[hsl(var(--muted))]" aria-label="Tutup" data-testid="button-close-form"><X size={19} /></button></div>
        <div className="grid gap-7 p-5 sm:p-7 lg:grid-cols-[1fr_290px]">
          <form onSubmit={(event) => { event.preventDefault(); if (canSave) onSave({ ...form, name: form.name.trim(), description: form.description.trim(), image: form.image.trim() }); }} className="space-y-4">
            <div><label className="mb-1.5 block text-xs font-bold text-[hsl(var(--foreground))]" htmlFor="product-name">Nama produk</label><input id="product-name" value={form.name} onChange={(event) => setField('name', event.target.value)} placeholder="Contoh: Teh Melati 25 kantong" className="focus-ring w-full rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3.5 py-3 text-sm outline-none focus:border-[hsl(var(--primary)/.55)]" data-testid="input-product-name" required /></div>
            <div className="grid gap-4 sm:grid-cols-2"><div><label className="mb-1.5 block text-xs font-bold" htmlFor="product-price">Harga (rupiah)</label><input id="product-price" type="number" min="1" value={form.price || ''} onChange={(event) => setField('price', Number(event.target.value))} placeholder="25000" className="focus-ring w-full rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3.5 py-3 text-sm outline-none focus:border-[hsl(var(--primary)/.55)]" data-testid="input-product-price" required /></div><div><label className="mb-1.5 block text-xs font-bold" htmlFor="product-category">Kategori</label><select id="product-category" value={form.category} onChange={(event) => setField('category', event.target.value)} className="focus-ring w-full rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3.5 py-3 text-sm outline-none focus:border-[hsl(var(--primary)/.55)]" data-testid="select-product-category">{categories.map((category) => <option value={category} key={category}>{category}</option>)}</select></div></div>
            <div><label className="mb-1.5 block text-xs font-bold" htmlFor="product-description">Deskripsi singkat</label><textarea id="product-description" value={form.description} onChange={(event) => setField('description', event.target.value)} placeholder="Ceritakan kenapa produk ini layak masuk keranjang..." rows={3} className="focus-ring w-full resize-none rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] px-3.5 py-3 text-sm outline-none focus:border-[hsl(var(--primary)/.55)]" data-testid="input-product-description" required /></div>
            <div><label className="mb-1.5 block text-xs font-bold" htmlFor="product-image">URL gambar</label><div className="relative"><ImagePlus className="absolute left-3 top-1/2 -translate-y-1/2 text-[hsl(var(--muted-foreground))]" size={17} /><input id="product-image" value={form.image} onChange={(event) => { setImageError(false); setField('image', event.target.value); }} placeholder="https://..." className="focus-ring w-full rounded-xl border border-[hsl(var(--input))] bg-[hsl(var(--background))] py-3 pl-10 pr-3.5 text-sm outline-none focus:border-[hsl(var(--primary)/.55)]" data-testid="input-product-image" /></div><p className="mt-1.5 text-[11px] text-[hsl(var(--muted-foreground))]">Bisa gunakan URL gambar publik. Kosongkan untuk latar warna otomatis.</p></div>
            <label className="flex cursor-pointer items-center gap-3 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--secondary)/.35)] p-3.5 text-sm font-semibold transition hover:bg-[hsl(var(--secondary))]"><input type="checkbox" checked={form.featured} onChange={(event) => setField('featured', event.target.checked)} className="size-4 accent-[hsl(var(--primary))]" data-testid="input-product-featured" /><Star size={16} className="text-[hsl(var(--accent))]" fill="currentColor" /> Tandai sebagai pilihan toko</label>
            <div className="flex justify-end gap-2 border-t border-[hsl(var(--border))] pt-5"><button type="button" onClick={onCancel} className="focus-ring rounded-full px-4 py-2.5 text-xs font-bold text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="button-cancel-form">Batal</button><button type="submit" disabled={!canSave} className="focus-ring inline-flex items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-5 py-2.5 text-xs font-bold text-[hsl(var(--primary-foreground))] transition hover:bg-[hsl(var(--primary)/.88)] disabled:cursor-not-allowed disabled:opacity-40" data-testid="button-save-product"><Save size={15} /> {isEditing ? 'Simpan perubahan' : 'Tambah produk'}</button></div>
          </form>
          <div className="hidden lg:block"><p className="mono-font mb-3 text-[10px] font-bold uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))]">Pratinjau kartu</p>{imageError || !form.image ? <div className="mb-4 grid aspect-[1.08] place-items-center overflow-hidden rounded-[22px] bg-[hsl(var(--secondary))] text-center text-[hsl(var(--primary))]"><Package size={30} /><span className="mt-2 block max-w-[150px] text-xs font-semibold">Gambar produk akan tampil di sini</span></div> : <img src={form.image} onError={() => setImageError(true)} className="mb-4 aspect-[1.08] w-full rounded-[22px] object-cover" alt="Pratinjau produk" />}<div className="pointer-events-none"><ProductCard product={preview} /></div></div>
        </div>
      </div>
    </div>
  );
}

export default function Admin() {
  const { products, isLoading, addProduct, updateProduct, deleteProduct } = useProducts();
  const [editing, setEditing] = useState<Product | null>(null);
  const [showForm, setShowForm] = useState(false);
  const [preview, setPreview] = useState<Product | null>(null);
  const [deleteTarget, setDeleteTarget] = useState<Product | null>(null);
  const [notice, setNotice] = useState('');
  const flash = (message: string) => { setNotice(message); window.setTimeout(() => setNotice(''), 2600); };
  const openAdd = () => { setEditing(null); setShowForm(true); };
  const save = (input: ProductInput) => { if (editing) { updateProduct(editing.id, input); flash('Perubahan produk sudah disimpan.'); } else { addProduct(input); flash('Produk baru masuk ke etalase.'); } setShowForm(false); };
  const remove = () => { if (deleteTarget) { deleteProduct(deleteTarget.id); flash(`${deleteTarget.name} dihapus dari etalase.`); setDeleteTarget(null); } };

  return (
    <div className="grain min-h-[100dvh] bg-[hsl(var(--background))]">
      <header className="border-b border-[hsl(var(--border))] bg-[hsl(var(--card)/.7)]"><div className="mx-auto flex max-w-[1240px] items-center justify-between px-5 py-4 lg:px-8"><Link href="/" className="focus-ring rounded-xl" data-testid="link-admin-brand"><BrandMark /><span className="ml-3 hidden border-l border-[hsl(var(--border))] pl-3 text-xs font-semibold text-[hsl(var(--muted-foreground))] sm:inline">Ruang toko</span></Link><Link href="/" className="focus-ring inline-flex items-center gap-2 rounded-full px-3 py-2 text-xs font-bold text-[hsl(var(--primary))] transition hover:bg-[hsl(var(--secondary))]" data-testid="link-back-store"><ArrowLeft size={15} /> Lihat toko</Link></div></header>
      <main className="mx-auto max-w-[1240px] px-5 pb-16 pt-9 lg:px-8 lg:pt-14">
        <div className="animate-rise-in flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mono-font text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--accent))]">Ruang toko</p><h1 className="display-font mt-2 text-[clamp(2.5rem,6vw,4.7rem)] font-bold leading-none tracking-[-.065em] text-[hsl(var(--primary))]">Etalase kamu.</h1><p className="mt-3 max-w-[450px] text-sm leading-relaxed text-[hsl(var(--muted-foreground))]">Atur produk yang muncul di Nixx store. Semua perubahan tersimpan otomatis di browser ini.</p></div><button onClick={openAdd} className="focus-ring inline-flex w-fit items-center gap-2 rounded-full bg-[hsl(var(--accent))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--accent-foreground))] shadow-[3px_3px_0_hsl(var(--primary))] transition hover:translate-x-0.5 hover:translate-y-0.5 hover:shadow-[1px_1px_0_hsl(var(--primary))]" data-testid="button-add-product"><Plus size={18} /> Tambah produk</button></div>
        <div className="mt-10 grid grid-cols-2 gap-3 sm:max-w-[500px]"><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4"><p className="mono-font text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Total produk</p><p className="display-font mt-2 text-3xl font-bold text-[hsl(var(--primary))]" data-testid="text-admin-total">{products.length}</p></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4"><p className="mono-font text-[10px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Pilihan toko</p><p className="display-font mt-2 text-3xl font-bold text-[hsl(var(--primary))]" data-testid="text-admin-featured">{products.filter((product) => product.featured).length}</p></div></div>
        <div className="mt-10 border-t border-[hsl(var(--border))] pt-6">{isLoading ? <div className="space-y-3">{[1, 2, 3].map((item) => <div className="skeleton h-[86px] rounded-2xl" key={item} />)}</div> : products.length ? <div className="space-y-3">{products.map((product, index) => <div className={`animate-rise-in delay-${Math.min(index + 1, 4)} flex flex-col gap-4 rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-3.5 shadow-[var(--shadow-sm)] transition hover:shadow-[var(--shadow-md)] sm:flex-row sm:items-center`} key={product.id} data-testid={`row-product-${product.id}`}><div className="flex min-w-0 flex-1 items-center gap-3"><div className="size-[66px] shrink-0 overflow-hidden rounded-xl bg-[hsl(var(--secondary))]">{product.image && <img src={product.image} alt="" className="size-full object-cover" />}</div><div className="min-w-0"><div className="flex items-center gap-2"><h2 className="truncate font-bold text-[hsl(var(--primary))]" data-testid={`text-admin-product-${product.id}`}>{product.name}</h2>{product.featured && <Star size={13} fill="currentColor" className="shrink-0 text-[hsl(var(--accent))]" />}</div><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{product.category} <span className="mx-1">/</span> <span className="font-bold text-[hsl(var(--foreground))]">{formatPrice(product.price)}</span></p></div></div><div className="flex items-center gap-1.5 border-t border-[hsl(var(--border))] pt-3 sm:border-0 sm:pt-0"><button onClick={() => setPreview(product)} className="focus-ring inline-flex items-center gap-1.5 rounded-full px-3 py-2 text-xs font-bold text-[hsl(var(--muted-foreground))] transition hover:bg-[hsl(var(--secondary))] hover:text-[hsl(var(--primary))]" data-testid={`button-preview-${product.id}`}><Eye size={15} /> Pratinjau</button><button onClick={() => { setEditing(product); setShowForm(true); }} className="focus-ring inline-flex items-center gap-1.5 rounded-full px-3 py-2 text-xs font-bold text-[hsl(var(--muted-foreground))] transition hover:bg-[hsl(var(--secondary))] hover:text-[hsl(var(--primary))]" data-testid={`button-edit-${product.id}`}><Edit3 size={15} /> Edit</button><button onClick={() => setDeleteTarget(product)} className="focus-ring inline-flex items-center gap-1.5 rounded-full px-3 py-2 text-xs font-bold text-[hsl(var(--destructive))] transition hover:bg-[hsl(var(--destructive)/.08)]" data-testid={`button-delete-${product.id}`}><Trash2 size={15} /> Hapus</button></div></div>)}</div> : <div className="rounded-[24px] border border-dashed border-[hsl(var(--border))] bg-[hsl(var(--card)/.55)] px-6 py-16 text-center"><Package className="mx-auto text-[hsl(var(--accent))]" size={38} /><h2 className="display-font mt-4 text-2xl font-bold text-[hsl(var(--primary))]" data-testid="text-empty-admin">Etalase masih kosong</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Tambahkan produk pertama supaya pelanggan bisa mulai melihat.</p><button onClick={openAdd} className="focus-ring mt-5 rounded-full bg-[hsl(var(--primary))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--primary-foreground))]" data-testid="button-empty-add-product">Tambah produk pertama</button></div>}</div>
      </main>
      {notice && <div className="animate-soft-pop fixed bottom-5 left-1/2 z-[60] flex -translate-x-1/2 items-center gap-2 rounded-full bg-[hsl(var(--primary))] px-4 py-3 text-xs font-bold text-[hsl(var(--primary-foreground))] shadow-[var(--shadow-lg)]" role="status" data-testid="status-admin-notice"><Check size={15} className="text-[hsl(var(--accent))]" /> {notice}</div>}
      {showForm && <ProductForm initial={editing ? { name: editing.name, price: editing.price, description: editing.description, image: editing.image, category: editing.category, featured: editing.featured } : emptyForm} onSave={save} onCancel={() => setShowForm(false)} isEditing={Boolean(editing)} />}
      {preview && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[hsl(var(--foreground)/.42)] p-5 backdrop-blur-sm" role="dialog" aria-modal="true" data-testid="dialog-product-preview"><div className="animate-soft-pop w-full max-w-[390px] rounded-[26px] bg-[hsl(var(--background))] p-4 shadow-[var(--shadow-lg)]"><div className="mb-3 flex items-center justify-between px-1"><p className="mono-font text-[10px] font-bold uppercase tracking-[.16em] text-[hsl(var(--accent))]">Pratinjau pelanggan</p><button onClick={() => setPreview(null)} className="focus-ring rounded-full p-2 text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="button-close-preview"><X size={18} /></button></div><ProductCard product={preview} /></div></div>}
      {deleteTarget && <div className="fixed inset-0 z-[55] flex items-center justify-center bg-[hsl(var(--foreground)/.42)] p-5 backdrop-blur-sm" role="dialog" aria-modal="true" data-testid="dialog-delete-product"><div className="animate-soft-pop w-full max-w-[380px] rounded-[24px] bg-[hsl(var(--card))] p-6 shadow-[var(--shadow-lg)]"><div className="grid size-11 place-items-center rounded-2xl bg-[hsl(var(--destructive)/.12)] text-[hsl(var(--destructive))]"><Trash2 size={20} /></div><h2 className="display-font mt-5 text-2xl font-bold tracking-[-.04em] text-[hsl(var(--primary))]">Hapus produk ini?</h2><p className="mt-2 text-sm leading-relaxed text-[hsl(var(--muted-foreground))]"><strong className="text-[hsl(var(--foreground))]">{deleteTarget.name}</strong> akan dihapus dari etalase dan tidak bisa dikembalikan.</p><div className="mt-6 flex justify-end gap-2"><button onClick={() => setDeleteTarget(null)} className="focus-ring rounded-full px-4 py-2.5 text-xs font-bold text-[hsl(var(--muted-foreground))] hover:bg-[hsl(var(--muted))]" data-testid="button-cancel-delete">Batal</button><button onClick={remove} className="focus-ring rounded-full bg-[hsl(var(--destructive))] px-4 py-2.5 text-xs font-bold text-[hsl(var(--destructive-foreground))]" data-testid="button-confirm-delete">Ya, hapus</button></div></div></div>}
    </div>
  );
}